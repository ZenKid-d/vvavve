/// Фасад сетевого обхода блокировок: реализация выбирается по платформе.
///
/// Веб — ветка по умолчанию, dart:io-реализация подключается условно: так новая
/// неподдержанная платформа деградирует, а не падает на этапе разрешения
/// импортов. В публичных сигнатурах фасада не должно быть типов из dart:io —
/// иначе он сам не скомпилируется под web (поэтому `buildDohDioAdapter`
/// возвращает базовый `HttpClientAdapter?`, а `buildDohHttpClient` живёт только
/// в io-реализации и наружу не выносится).
library;

export 'doh_http_web.dart' if (dart.library.io) 'doh_http_io.dart';
