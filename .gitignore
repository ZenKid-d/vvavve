# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Release signing — НИКОГДА не коммитить ключ и пароли
key.properties
android/key.properties
*.jks
*.keystore

# Obfuscation symbols (генерируются при --split-debug-info в релизе).
# Нужны для декодирования стек-трейсов крашей; хранятся локально/в CI
# как артефакт, в репозиторий не идут — могут утечь имена Dart-символов.
build/symbols/
*.symbols

# Release APK + sha256 (генерируются в релизном workflow локально/в CI)
roundds-*.apk
roundds-*.apk.sha256

# Служебные директории редактора/окружения — не относятся к проекту
.omo/
.zcode/

# l10n: генерируется автоматически из lib/l10n/*.arb при `flutter pub get`
# (flutter: generate: true в pubspec.yaml) — не коммитим производные файлы.
lib/l10n/gen/

# sqlite3 для веба: кладёт `dart run sqflite_common_ffi_web:setup` (см.
# docs/WEB_AND_SYNC.md), в CI это отдельный шаг. Почти мегабайт бинарников,
# привязанных к версии пакета, — в репозитории им делать нечего.
web/sqlite3.wasm
web/sqflite_sw.js

